-- SELLO Database Initialization Script
-- The FastAPI backend uses SQLAlchemy to automatically generate tables,
-- so this script acts as a placeholder for initial data/settings if needed.
SELECT 1;
